import { grey, red, orange, yellow, green, blue, purple } from './palette';

const colors = {
  gray: {
    ...grey,
  },
  red,
  orange,
  yellow,
  green,
  blue,
  purple,
};

export default colors;
